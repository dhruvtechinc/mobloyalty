<% if @vendor_program_setup_bp %>
<%= form_for (@vendor_program_setup_bp), html: {class: "text-left"} do |f| %>
<fieldset>
  <% if @vendor_program_setup_bp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vendor_program_setup_bp.errors.count, "error") %> prohibited this vendor_program_setup_bp from being saved:</h2>

      <ul>
      <% @vendor_program_setup_bp.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <h4>Birthday Points Setup - how many points does a customer accrue and when and what they redeem on their</h4>
  <div class="form-group">
  <%= f.hidden_field :vendor_programs_id, :value => 3 %>
    <%= f.label :accrue_points_visits, "How many bonus points on Birthday?" %>
    <%= f.number_field :accrue_points_visits, class: "form-control", input_html: {value: ''}, placeholder: "Enter Number of points. Enter 0 if none." %>
  </div>
  <div class="form-group">
    <%= f.label :reward_type_desc, "Describe the reward that customer will receive" %>
    <%= f.text_field :reward_type_desc, class: "form-control", input_html: {value: ''}, placeholder: "Enter Description" %>
  </div>
  <div class="btn-group">
    <%= f.submit "Setup", class: "btn btn-lg btn-info", name: "bonus_points_setup" %>
  </div>
  <div class="btn-group">
    <%= f.submit "Clear", class: "btn btn-lg", name: "clear", type: "reset" %>
  </div>
  </fieldset>
<% end %>
<% else %>
<h1> hello </h1>
<% end %>
 